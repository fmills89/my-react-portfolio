import React from 'react';
import photo from '../../assets/projects/hero-bg.jpeg';

function Project() {
    const currentProject = {
       
    };
    return (
        <section>
            <h1>{currentProject.name}</h1>
            <p>{currentProject.name}</p>
            <div className='flex-row'>
                g
            </div>
        </section>
    );
}

export default Project;